const Dashboard = () => {
  return (
    <div style={styles.container}>
      <h2>Dashboard</h2>
      <p>Here you will see your workouts and stats later.</p>
    </div>
  );
};

const styles = {
  container: {
    padding: '2rem',
    textAlign: 'center'
  }
};

export default Dashboard;
