# Feature Status Inventory

Verified on August 5, 2026 from the current repository. Status terms are used consistently across the docs: **Implemented and verified in the repository**, **Implemented but release or deployment status not verified**, **Partially implemented**, **In development**, **Planned**, **Deprecated or no longer used**, and **Unable to verify**.

| Area | Feature | Status | Frontend evidence | Backend evidence | Database evidence | Deployment evidence | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Authentication | Register | Implemented and verified in the repository | `client/src/pages/Register.jsx` | `server/src/routes/authRoutes.js`, `server/src/routes/userRoutes.js`, `server/src/services/authService.js` | `server/src/models/User.js` | Env examples include JWT secrets | Frontend posts to `/api/users/register`; `/api/auth/register` also exists. |
| Authentication | Login | Implemented and verified in the repository | `client/src/pages/Login.jsx`, `client/src/context/AuthContext.jsx` | `server/src/controllers/authController.js` | `User.password` with `select: false` | Not deployment verified | Access token is stored in `localStorage`; refresh cookie is set when refresh secret exists. |
| Authentication | Refresh token | Implemented and verified in the repository | `client/src/services/api.js` | `POST /api/auth/refresh` | No refresh-token persistence model | Not deployment verified | Refresh token is a signed cookie, not stored server-side. |
| Authentication | Logout | Implemented and verified in the repository | `AuthContext.logout`, `Header.jsx` | `POST /api/auth/logout` | Not applicable | Not deployment verified | Clears local storage and refresh cookie. |
| Authorization | Protected frontend routes | Implemented and verified in the repository | `ProtectedRoute.jsx`, `PublicRoute.jsx` | Not applicable | Not applicable | Not deployment verified | Redirects guests to `/login` and authenticated users away from auth pages. |
| Authorization | Backend bearer-token protection | Implemented and verified in the repository | `api.js` adds `Authorization: Bearer` | `server/src/middleware/auth.js` | `User` lookup | Not deployment verified | Uses JWT payload `userId`. |
| Authorization | Workout ownership | Implemented and verified in the repository | Dashboard/edit/session call user-owned IDs | `workoutService.ensureWorkoutOwner` | `Workout.author` | Not deployment verified | Enforced for get/update/delete/duplicate and session creation. |
| Authorization | Profile ownership | Implemented and verified in the repository | Dashboard fetches `/api/users/:id` for current user | `userService.ensureOwnProfile` | `User` | Not deployment verified | No admin role or user listing endpoint. |
| Authorization | Session ownership | Implemented and verified in the repository | `History.jsx`, `Progress.jsx`, `Dashboard.jsx` | session queries filter `user: req.user._id` | `WorkoutSession.user` | Not deployment verified | Create checks workout owner before saving. |
| User profile | Fetch profile | Implemented and verified in the repository | `Dashboard.jsx` | `GET /api/users/:id` | `User` | Not deployment verified | Profile update/delete APIs exist but no dedicated frontend profile page was found. |
| User profile | Update/delete profile | Partially implemented | No dedicated UI found | `PUT/DELETE /api/users/:id` | `User` | Not deployment verified | Backend exists; product UI not confirmed. |
| Navigation | Header and auth-aware nav | Implemented and verified in the repository | `Header.jsx`, `App.css` | Not applicable | Not applicable | Not deployment verified | Authenticated nav includes Dashboard, History, Goals, Progress, Records, Exercises. |
| Responsive navigation | Horizontal secondary nav and wrapping header | Implemented and verified in the repository | `App.css` breakpoints | Not applicable | Not applicable | Not deployment verified | No hamburger menu; nav scrolls horizontally. |
| Theme | Light/dark/system theme | Implemented and verified in the repository | `ThemeContext.jsx`, `ThemeToggle.jsx`, `index.css` | Not applicable | Not applicable | Not deployment verified | Preference is local-only in `localStorage`; not saved to user profile. |
| Dashboard | Stats and routine list | Implemented and verified in the repository | `Dashboard.jsx` | `/api/workouts`, `/api/sessions/summary`, `/api/goals/summary` | `Workout`, `WorkoutSession`, `Goal` | Not deployment verified | Pulls multiple API sources. |
| Dashboard | Demo insight fallback | Implemented and verified in the repository | `ProgressInsights.jsx` | Not applicable | Not applicable | Not applicable | UI placeholder when no sessions exist; not persisted data. |
| Routines | Create routine | Implemented and verified in the repository | `CreateWorkout.jsx`, `WorkoutBuilder.jsx` | `POST /api/workouts` | `Workout` | Not deployment verified | Supports name, duration, difficulty, notes, cover image URL, exercises, rest seconds, exercise notes. |
| Routines | Edit routine | Implemented and verified in the repository | `EditWorkout.jsx` | `PUT /api/workouts/:id` | `Workout` | Not deployment verified | Edit preserves existing cover image but does not expose replacement upload. |
| Routines | Delete routine | Implemented and verified in the repository | `Dashboard.jsx` | `DELETE /api/workouts/:id` | `Workout` | Not deployment verified | Does not cascade delete completed sessions; sessions keep references and workout name snapshot. |
| Routines | Duplicate routine | Implemented and verified in the repository | `Dashboard.jsx` | `POST /api/workouts/:id/duplicate` | `Workout` | Not deployment verified | Name suffix ` Copy`, capped by service logic. |
| Exercises | Default library | Implemented and verified in the repository | `WorkoutBuilder.jsx`, `Exercises.jsx` | `exerciseController.getExercises` | `Exercise`; also in-memory `defaultExercises` | Seed docs and scripts | Defaults are returned even before MongoDB seeding. |
| Exercises | Custom exercise create | Implemented and verified in the repository | `Exercises.jsx` | `POST /api/exercises` | `Exercise.createdBy` | Not deployment verified | No update/delete endpoints. |
| Exercise images | Routine cover upload | Implemented and verified in the repository | `ImageUpload.jsx`, `CreateWorkout.jsx` | `POST /api/upload`, Cloudinary config | `Workout.coverImage` stores URL | Cloudinary env examples | Upload validates type and size; public ID is returned but not stored on `Workout`. |
| Exercise images | Per-exercise image/icon | Planned or discussed, but no complete implementation was found | Not found | Not found | No field | Not found | Only routine cover images exist. |
| Workout completion | Manual set logging | Implemented and verified in the repository | `ActiveSession.jsx` | `POST /api/sessions` | `WorkoutSession.exercises.sets` | Not deployment verified | Requires at least one completed set. |
| Workout completion | Rest timer | Implemented and verified in the repository | `ActiveSession.jsx` | Not applicable | Not stored | Not applicable | Client-side countdown only. |
| History | Completed-session list | Implemented and verified in the repository | `History.jsx` | `GET /api/sessions` | `WorkoutSession` | Not deployment verified | Supports pagination, filters, sort. |
| History | CSV export | Implemented and verified in the repository | `History.jsx` | `GET /api/sessions/export.csv` | `WorkoutSession` | Not deployment verified | One row per logged set. |
| Calendar | Month summary and date selection | Implemented and verified in the repository | `History.jsx` | `GET /api/sessions/calendar`, `GET /api/sessions?from=to=` | `WorkoutSession.completedAt` | Not deployment verified | Uses UTC date keys for month cells. |
| Streaks | Current and longest streak | Implemented and verified in the repository | `Goals.jsx`, `Dashboard.jsx`, `AchievementSection.jsx` | `goalController`, `sessionController` | `WorkoutSession.completedAt`, `Goal` | Not deployment verified | Streaks are derived, not stored. |
| Progress summaries | Exercise progress search | Implemented and verified in the repository | `Progress.jsx` | `GET /api/sessions/progress` | `WorkoutSession` | Not deployment verified | Exact case-insensitive exercise name search. |
| Personal records | Automatic best records | Implemented and verified in the repository | `Records.jsx`, session save toast | `recordService.updatePersonalRecordsForSession` | `PersonalRecord` | Not deployment verified | Max weight, max reps, max volume per exercise. |
| Goals | Weekly goal update | Implemented and verified in the repository | `Goals.jsx` | `GET/PUT /api/goals/current`, `GET /api/goals/summary` | `Goal` | Not deployment verified | Target must be 1 to 14. |
| Empty states | Core empty states | Implemented and verified in the repository | Dashboard, History, Progress, Records, Exercises | Not applicable | Not applicable | Not deployment verified | Most major pages show empty-state copy. |
| Error states | Toasts and inline alerts | Implemented and verified in the repository | Pages call `toast.error` and render alerts | `errorHandler.js` | Not applicable | Not deployment verified | Error format is `{ success: false, message }`. |
| Loading states | Route/page/component loading states | Implemented and verified in the repository | `LoadingSpinner.jsx`, page-specific spinners | Not applicable | Not applicable | Not deployment verified | Present across protected routing and data pages. |
| Form validation | Auth/workout/goal/image validation | Implemented and verified in the repository | `Register.jsx`, `Login.jsx`, `WorkoutBuilder.jsx`, `Goals.jsx`, `ImageUpload.jsx` | validators and manual checks | Mongoose schemas | Not deployment verified | Exercise library server validation is manual and simpler than workout validation. |
| Seed data | Demo seed | Implemented and verified in the repository | Docs reference demo users | `server/scripts/seedDemo.js` | All main models | Not deployment verified | Guarded against accidental production use. |
| Seed data | Default exercise seed | Implemented and verified in the repository | Exercise library uses defaults without seed | `seedExercises.js`, `defaultExercises.js` | `Exercise` | Not deployment verified | Optional persistence script. |
| Accessibility | Labels, focus states, aria live | Partially implemented | Forms, calendar buttons, spinners, focus CSS | Not applicable | Not applicable | Not verified by audit tooling | No automated accessibility tests found. |
| Testing | Backend integration tests | Implemented and verified in the repository | Not applicable | `server/tests/auth.test.js` | Test DB env support | Not run | File covers many API domains despite name. |
| Testing | Frontend tests | Partially implemented | `Header.test.jsx`, `Login.test.jsx` | Not applicable | Not applicable | Not run | Limited component/page coverage. |
| Deployment | Docker local/prod compose | Implemented but release or deployment status not verified | `client/Dockerfile`, `nginx.conf` | `server/Dockerfile` | Mongo Compose service | `docker-compose*.yml` | Config exists; external deployment status not confirmed. |
| Deployment | Vercel SPA fallback | Implemented but release or deployment status not verified | `client/vercel.json` | Not applicable | Not applicable | Vercel config only | No production deployment dashboard inspected. |
| SEO | SPA metadata | Partially implemented | `client/index.html`, `public/favicon.svg` | Not applicable | Not applicable | Vercel config | No route-specific SEO. |
| OAuth | OAuth login | Planned or discussed, but no complete implementation was found | Not found | Not found | Not found | Not found | Do not claim OAuth exists. |
| Reminders | Workout reminders/notifications | Planned or discussed, but no complete implementation was found | Not found | Not found | No reminder model | Not found | No scheduled jobs or notification service. |
| Real-time | Socket.IO connection | Partially implemented | `Dashboard.jsx`, `services/socket.js` | `server.js` Socket.IO auth | `User` lookup | Socket env example | Connection exists but no product events are emitted or displayed. |
| Legacy/unused | `ConnectionTest` component | Deprecated or no longer used | `client/src/components/common/ConnectionTest.jsx` exists, no imports found | `/api/health` exists | Not applicable | Not applicable | Utility component is present but unused. |
| Legacy/unused | Vite/React template assets | Deprecated or no longer used | `client/src/assets/react.svg`, `vite.svg`, `client/README.md` | Not applicable | Not applicable | Not applicable | Assets/readme remain from template and are not core product docs. |

